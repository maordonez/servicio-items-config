spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_springboot_cloud?serverTimezone=America/Bogota
    username: root
    password: 12345678
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    database-platform: org.hibernate.dialect.MySQL8Dialect
    hibernate:
      ddl-auto: create

logging:
  level:
    org.hibernate.sql: debug
